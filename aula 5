const produto01 = {
  nome: "Arroz",
  preco: 25,
  estoque: 40,
};

console.log(`${produto01.nome} custa R$ ${produto01.preco} e tem ${produto01.estoque} em estoque`);

const produto = {
  nome: "Arroz",
  preco: 25,
  estoque: 40,
};

produto.preco = produto.preco * 1.10;
produto.promocao = true;

console.log(produto);

function descricao(produto) {
  return `${produto.nome} custa R$ ${produto.preco} e tem ${produto.estoque} em estoque`;
}

const produto1 = {
  nome: "Arroz",
  preco: 25,
  estoque: 40,
};

const produto2 = {
  nome: "Feijão",
  preco: 12,
  estoque: 30,
};

console.log(descricao(produto1));
console.log(descricao(produto2));

const produtos = [
  { nome: "Óleo", preco: 8 },
  { nome: "Café", preco: 35 },
];

for (const produto of produtos) {
  console.log(`${produto.nome} - R$ ${produto.preco}`);
}

const produtos = [
  { nome: "Televisão", preco: 1800 },
  { nome: "Cadeira", preco: 120 },
];

const caros = produtos.filter(produto => produto.preco > 50);
console.log(caros);

const nomes = caros.map(produto => produto.nome);
console.log(nomes);

const turma = [
  { nome: "Ana", nota: 8 },
  { nome: "Bruno", nota: 5 },
  { nome: "Carla", nota: 9 },
  { nome: "Diego", nota: 6 },
  { nome: "Eduarda", nota: 7 },
];

const aprovados = turma
  .filter(aluno => aluno.nota >= 6)
  .map(aluno => aluno.nome);

console.log(aprovados);

let soma = 0;

for (const aluno of turma) {
  soma += aluno.nota;
}

const media = soma / turma.length;
console.log("Média da turma:", media);

const acimaDaMedia = turma.filter(aluno => aluno.nota > media);
console.log("Acima da média:", acimaDaMedia.length);
